local ls = require("luasnip")
local s = ls.snippet
local t = ls.text_node
local i = ls.insert_node

return {
    -----------------------------------------------------------
    -------------------- Header Snipped -----------------------
    -----------------------------------------------------------
    s("header", {
        t({ "-----------------------------------------------------------", "" }),
        t({ "--------------------" }),
        i(1, " <title> "),
        t({ "--------------------", "" }),
        t({ "-----------------------------------------------------------" }),
    }),
}
